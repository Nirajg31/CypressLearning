{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "Nik cypress automation",
  "main": "index.js",
  "scripts": 
  {
    "test": "npx cypress run",
    "headTest":"npx cypress run --headed",
    "chrome":"npx run test --browser chrome",
    "recordDashboardTest":"npx cypress run  --record --key c497c7fd-7ccc-4c27-9fad-efd3bff6f293",
    "specificFile": "cypress run --spec \"cypress/integration/examples/Testframework1.cy.js\" --headed --browser chrome",
    "smokeTest":"npx cypress run --spec \"cypress/integration/examples/Testframework1.cy.js\""
  //   "cypress:open": "cypress open",       // Opens Cypress Test Runner
  // "cypress:run": "cypress run",         // Runs Cypress tests in headless mode
  // "cypress:run:chrome": "cypress run --browser chrome",  // Runs tests in Chrome
  // "cypress:run:firefox": "cypress run --browser firefox",  // Runs tests in Firefox
  // "cypress:run:edge": "cypress run --browser edge",  // Runs tests in Edge
  // "cypress:run:electron": "cypress run --browser electron",  // Runs tests in Electron
  // "cypress:run:headed": "cypress run --headed",  // Runs Cypress tests in headed mode
  // "cypress:run:ci": "cypress run --record",  // Runs Cypress tests and records results
  // "cypress:run:ci:headed": "cypress run --record --headed",  // Runs Cypress tests in headed mode and records results
  // "cypress:run:ci:chrome": "cypress run --record --browser chrome",  // Records tests in Chrome
  // "cypress:run:ci:firefox": "cypress run --record --browser firefox",  // Records tests in Firefox
  // "cypress:open:ci": "cypress open --record",  // Opens Cypress Test Runner in CI mode


  },
  "author": "Niraj",
  "license": "ISC",
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "cypress": "^13.11.0",
    "cypress-iframe": "^1.0.1",
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
